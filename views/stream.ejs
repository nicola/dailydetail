<% include partials/head %>

<div class="container">
  <header>
    <div class="masthead">
      <% include partials/user_actions %>
      <h3 class="muted"><%- linkUser(stream.user) %> / <%- linkStream(stream)%></h3>
      <% include partials/nav %>
    </div>
  </header>
  <div id="wrapper">
    <% if (stream.pictures.length === 0) { %>
    No picture, yet<% if (user) { %> <a>upload your first</a> <% } %>!
    <% } %>
  </div>
  <footer></footer>
</div>

<% include partials/foot %>